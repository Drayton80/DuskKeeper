PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL via Docker)
DATABASE_URL="postgresql://duskkeeper:duskkeeper_password@localhost:5432/duskkeeper"

# MeiliSearch Configuration (via Docker)
MEILI_HOST=http://localhost:7700
MEILI_API_KEY=duskkeeper_master_key
